name: medens
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.5.4

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  # Integrating and syncing data from smart wearable devices.
  health: ^11.1.1
  # Processing and analyzing uploaded images.
  image_picker: ^1.1.2
  flutter_image_compress: ^2.3.0
  flutter_svg: ^2.0.16
  # Utilizing machine learning models for sensor-based diagnostics.
  google_ml_kit: ^0.19.0
  tflite_flutter: ^0.11.0
  # Identifying top services in the user's vicinity.
  google_maps_flutter: ^2.10.0
  #  Firebase configuration files
  firebase_core: ^3.9.0
  firebase_analytics: ^11.3.6
  firebase_auth: ^5.3.4
  cloud_firestore: ^5.6.0
  firebase_messaging: ^15.1.6
  firebase_crashlytics: ^4.2.0
  firebase_ml_model_downloader: ^0.3.1+6
  firebase_performance: ^0.10.0+11

dev_dependencies:
  flutter_test:
    sdk: flutter

  # To add assets to your application, add an assets section, like this:
  # assets:
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg

  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
